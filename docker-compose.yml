services:
  bighoe:
    build: .
    container_name: bighoe
    environment:
      HOST: 0.0.0.0
      PORT: 5173
    ports:
      - "5173:5173"
    volumes:
      - ./bighoe.db:/app/bighoe.db
    restart: unless-stopped
